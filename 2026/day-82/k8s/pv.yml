apiVersion: v1
kind: PersistentVolume
metadata:
  name: bankapp-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: gp2
  awsElasticBlockStore:
    volumeID: <volume-id>
    fsType: ext4
