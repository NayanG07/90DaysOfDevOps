server:
  name: web-01
  ip: 192.168.1.100
  port: 8080

database:
  host: db.internal
  name: appdb
  credentials:
    user: admin
    password: secret123

startup_script: |
  #!/bin/bash
  systemctl start nginx
  systemctl enable nginx
  echo "Server ready"

config_description: >
  This is a folded multi-line string that
  will be joined into a single line by the
  YAML parser. Useful for long descriptions.
